//==============================
//? bus stations enum
//==============================

export enum status{
   covered = 'covered',
   notCovered = 'notCovered'
}

// default stations enum
export enum defaultType{
   start = 'START',
   end = 'END',
   notDefault = 'NOT_DEFAULT'
}